---
- name: Setting up a devops system
  hosts: localhost
  connection: local
  become: true

  vars:
    - system_purpose: "ansible_dev"

  vars_files:
    - vars/default.config.yaml

  roles:
    - base
    - ansible_dev
    - harden
