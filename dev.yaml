---
- name: Setting up a development system
  hosts: localhost
  connection: local
  become: true

  vars:
    - system_purpose: "dev"

  vars_files:
    - vars/default.config.yaml

  roles:
    - prep
    - base
    - dev
    - harden
